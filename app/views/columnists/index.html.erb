 <% if session[:user_id] %>

  <% @current_user ||= User.find(session[:user_id]) if session[:user_id] %>

   <%= render 'layouts/header' %>
   <div class="content-wrapper">
    <!-- Content Header (Page header) -->


    <!-- Main content -->
    <section class="content">
<div class="row">
  <div class="col-md-12">
      <div class="boxed">
                <!-- Title Bart Start -->

          


<h2>Listado de Columnistas <a href="/columnists/new" style="font-size: 16px;">Cargar Columnista</a></h2>

    <div class="inner no-radius">


  <table class="table table-striped table-bordered table-hover table-full-width" id="sample_1">
                  <thead>
                    <tr>
                            <th>ID</th>
                            <th>Foto</th>
                            <th>Nombre</th>
                            <th>Facebook</th>
                            <th>Instagram</th>
                            <th>Twitter</th>
                            <th>Linkedin</th>
                            <th>Semblanza</th>
                  
                          
                             <th colspan="3"></th>
                    </tr>
                  </thead>
                  <tbody>
  
  <% @columnists.each do |columnist| %>
      <tr>
        <td><%= columnist.colm_id %></td>
  <td><img src="<%= columnist.picture.url %>" style="width:70px;height:70px;border-radius: 50%;"></td>
        <td><%= columnist.full_name %></td>
          <td><%= columnist.facebook %></td>
          <td><%= columnist.instagram %></td>
          <td><%= columnist.twitter %></td>
          <td><%= columnist.linkedin %></td>
              <td><%= columnist.semblanza %></td>


 
<td><a href="/editcolumnist?columnist_id=<%= columnist.colm_id %>">Editar</a></td>
        <td><a href="/eliminarcolumnist?columnist_id=<%= columnist.colm_id %>">Eliminar</a></td>
      </tr>
    <% end %>



                  </tbody>
                </table>



        </div>


      </div>
</div>
</div>





</section>
</div>
<% else %><br><br><br><br>
<div align="center">

<h2>Cerraste tu sesión, vuelve a logearte</h2>
</div>
<br><br><br><br>
<% end %>
