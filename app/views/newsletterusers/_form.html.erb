<%= form_with(model: newsletteruser, local: true) do |form| %>
  <% if newsletteruser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newsletteruser.errors.count, "error") %> prohibited this newsletteruser from being saved:</h2>

      <ul>
      <% newsletteruser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :newsletteruser_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :newsletteruser_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
